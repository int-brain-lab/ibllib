`brainbox.processing`: Functions for transforming data
===============================================================

The `processing` module provides low-level utilities for processing data, such as functionality
for binning spikes into non-overlapping bins, or the convolution of spike trains with a filter.

.. automodapi:: brainbox.processing.processing
    :no-heading:
    :no-main-docstr:
